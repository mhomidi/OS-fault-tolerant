//
// Created by Hadi Omidi on 3/23/19.
//

#include "functions.h"

bool fexists(const char *filename) {
    ifstream ifile(filename);
    return (bool)ifile;
}

vector<string> files() {
    int number = 0;
    int folderName = 0;
    int fileName = 0;
    while (true){
        while(true) {

            fileName++;
        }
        folderName++;
    }
}